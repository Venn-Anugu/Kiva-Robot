class supervisor:
    def _init_(ID, Name):
        self.ID = ID
        self.Name = Name
    def SendOrderToRobot(x):
        print('Supervisor using cluter computers sends order details to the processing unit')
    def ConfirmOrder(x):
        print('Supervisor confirms the order if the order picked up by the robot is right')
    def Ship(x):
        print('Supervisor packs and ships the order')